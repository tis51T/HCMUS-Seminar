# Labeling

First going to `soucre\labeling\pyabsa_call\triple_extract\model_call.py` to run pyABSA first. The result will be store at `.\triplet_batch\`. Then going back to root dir and run `source\labeling\ollama_call.py` to extract the Aspect Category.


# SMF
Run `object_detection` -> `image_caption` -> `compute_sim` -> `matching`